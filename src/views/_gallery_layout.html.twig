{# _gallery_layout.html.twig #}
{% extends standalone ? '_layout.html.twig' : 'set.html.twig' %}
{% import '_macros.html.twig' as macros %}
{% set header_attr = ' class="loadme"' %}
{% block content %}
    {% if standalone %}
        <div id="container">
    {% endif %}
        <article class="loadme">
            <h2><a href="{{ domain ~ set.smak_subpath ~ '/' ~ set.name }}.html" title="{{ title|raw }}">{{ title|raw }}</a>{% if set.is_fresh %} <small>NEW!</small>{% endif %}</h2>
            {% block intro %}{% endblock %}
            {% if standalone %}
                {% block body %}{% endblock %}
                {% if 0 < nav|length %}
                    <nav>
                        <ul>
                            <li id="next">{% if nav.next %}&laquo; <a href="{{ domain ~ nav.next.smak_subpath ~ '/' ~ nav.next.name }}.html" title="Go the next set">next entry</a>{% endif %}</li>
                            <li id="prev">{% if nav.prev %}<a href="{{ domain ~ nav.prev.smak_subpath ~ '/' ~ nav.prev.name }}.html" title="Go the previous set">previous entry</a> &raquo;{% endif %}</li>
                        </ul>
                    </nav>
            {% endif %}
            {% else %}
                <p><small>{{ set.count() }} photos</small></p>
            {% endif %}
        </article>
    {% if standalone %}
        </div>
    {% endif %}
{% endblock %}